\echo === ????? ??????? ????????? ===
SELECT status, count(*) FROM imported_records GROUP BY status;

\echo === ????? ???????/????? (?????? ??????) ===
SELECT scope, code, field, message, count(*) AS n
FROM import_issues
GROUP BY scope, code, field, message
ORDER BY n DESC
LIMIT 25;

\echo === ???? ?? ?? ??? ????? ?????? ????? ???? ===
SELECT "sourceRowNo", status, "rawCells"
FROM imported_records
ORDER BY "sourceRowNo"
LIMIT 2;
